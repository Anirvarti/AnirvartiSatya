{
  "entities": {
    "Photo": {
      "title": "Photo",
      "description": "A secure photo uploaded to the user's gallery",
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "URL of the image"
        },
        "caption": {
          "type": "string",
          "description": "Descriptive caption or notes for the photo"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the photo was cataloged"
        },
        "ownerId": {
          "type": "string",
          "description": "Firebase Auth UID of the creator"
        },
        "ownerEmail": {
          "type": "string",
          "description": "Email address of the creator"
        }
      },
      "required": ["url", "caption", "createdAt", "ownerId", "ownerEmail"]
    },
    "Permission": {
      "title": "Permission",
      "description": "An email authorized to view the secure photo gallery",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "Gmail address to permit"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the permission was granted"
        }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "/photos/{photoId}": {
      "schema": "Photo",
      "description": "Collection of gallery photo documents"
    },
    "/permissions/{permissionId}": {
      "schema": "Permission",
      "description": "List of emails granted secure gallery access"
    }
  }
}
